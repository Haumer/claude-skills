---
description: Alias for /walkthrough — annotated live product tour in the browser, DB-verified each step, bugs logged. Usage: /wt <journey> [as <persona>]
---

# /wt

Short alias for the `walkthrough` skill.

Invoke the `walkthrough` skill now via the Skill tool, passing `$ARGUMENTS`
through unchanged as its args. Follow that skill's instructions in full —
do not improvise a walkthrough from this file.
